{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Expo application",
            "type": "reactnativedirect",
            "request": "attach",
            "cwd": "${workspaceFolder}",
            "port": 19001 // Porta padrão para projetos Expo
        }
    ]
}
